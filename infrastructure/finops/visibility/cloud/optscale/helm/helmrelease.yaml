apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-cost-metrics-collector
  namespace: optscale
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-cost-metrics-collector
      version: 0.1.2
      sourceRef:
        kind: HelmRepository
        name: hystax
        namespace: flux-system
  values:
  # https://github.com/hystax/helm-charts/blob/main/charts/kube-cost-metrics-collector/values.yaml