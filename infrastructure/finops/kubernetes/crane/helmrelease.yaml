apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: crane
  namespace: crane
spec:
  interval: 5m
  chart:
    spec:
      chart: crane
      version: 0.11.0
      sourceRef:
        kind: HelmRepository
        name: crane
        namespace: flux-system
  values:
  # https://github.com/gocrane/helm-charts/blob/main/charts/crane/values.yaml