apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gpu-provisioner
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: v0.3.3
  url: https://github.com/Azure/gpu-provisioner.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/charts/gpu-provisioner