apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: security-profiles-operator
  namespace: security-profiles-operator
spec:
  interval: 5m
  chart:
    spec:
      chart: deploy/helm/
      sourceRef:
        kind: GitRepository
        name: security-profiles-operator
        namespace: flux-system
  values:
  # https://github.com/kubernetes-sigs/security-profiles-operator/blob/main/deploy/helm/values.yaml
    replicaCount: 1
    