apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: stackrox-central-services
  namespace: stackrox
spec:
  interval: 5m
  chart:
    spec:
      chart: stackrox-central-services
      version: 400.5.5
      sourceRef:
        kind: HelmRepository
        name: stackrox
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/stackrox/stackrox-central-services
