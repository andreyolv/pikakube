apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rbac-manager
  namespace: rbac-manager
spec:
  interval: 5m
  chart:
    spec:
      chart: rbac-manager
      version: 1.21.0
      sourceRef:
        kind: HelmRepository
        name: fairwinds-stable
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/fairwinds-stable/rbac-manager
  # https://github.com/FairwindsOps/charts/blob/master/stable/rbac-manager/values.yaml