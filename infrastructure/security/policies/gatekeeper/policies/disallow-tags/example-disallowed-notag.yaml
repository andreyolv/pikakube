apiVersion: v1
kind: Pod
metadata:
  name: opa-disallowed-notags
  namespace: teste
spec:
  containers:
    - name: opa
      image: openpolicyagent/opa
      args:
        - "run"
        - "--server"
        - "--addr=localhost:8080"
