apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: oathkeeper
  namespace: oathkeeper
spec:
  interval: 5m
  chart:
    spec:
      chart: oathkeeper
      version: 0.47.0
      sourceRef:
        kind: HelmRepository
        name: ory
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/ory/oathkeeper
  # https://github.com/ory/k8s/blob/master/helm/charts/oathkeeper/values.yaml
