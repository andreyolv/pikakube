apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-init
  namespace: vault
data:
  vault-init.sh: |
    echo "Waiting for Vault to be ready..."
    sleep 5

    vault operator unseal $VAULT_AUTO_UNSEAL_KEY_0