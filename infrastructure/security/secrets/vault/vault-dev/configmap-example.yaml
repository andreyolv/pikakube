#apiVersion: v1
#kind: ConfigMap
#metadata:
#  name: vault-init
#  namespace: vault
#data:
#  vault-init.sh: |
#    echo "Waiting for Vault to be ready..."
#    sleep 5
#
#    echo "Enabling KV secrets engine at path 'pikakube'..."
#    vault secrets enable -path=pikakube -version=2 kv
#
#    echo "Adding secrets..."
#    vault kv put pikakube/variables/hello value=world
#    vault kv put pikakube/variables/hello2 value=world2
#
#
#    echo "Vault initialization script completed."