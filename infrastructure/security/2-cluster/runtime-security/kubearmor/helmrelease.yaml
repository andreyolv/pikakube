apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubearmor
  namespace: kubearmor
spec:
  interval: 5m
  chart:
    spec:
      chart: kubearmor-operator
      version: v1.2.0
      sourceRef:
        kind: HelmRepository
        name: kubearmor
        namespace: flux-system
  values:
  # https://github.com/kubearmor/KubeArmor/blob/main/deployments/helm/KubeArmorOperator/values.yaml

