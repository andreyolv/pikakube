apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: falco-talon
  namespace: falco
spec:
  interval: 5m
  dependsOn:
    - name: falco
      namespace: falco
  chart:
    spec:
      chart: falco-talon
      version: 0.3.0
      sourceRef:
        kind: HelmRepository
        name: falcosecurity
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/falcosecurity/falco-talon
  # https://github.com/falcosecurity/charts/blob/master/charts/falco-talon/values.yaml
    