apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: infisical
  namespace: infisical
spec:
  interval: 5m
  chart:
    spec:
      chart: secrets-operator
      version: 0.8.0
      sourceRef:
        kind: HelmRepository
        name: infisical-helm-charts
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/infisical/secrets-operator
  # https://github.com/Infisical/infisical/blob/main/helm-charts/secrets-operator/values.yaml