apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tracee
  namespace: tracee
spec:
  interval: 5m
  chart:
    spec:
      chart: tracee
      version: 0.20.0
      sourceRef:
        kind: HelmRepository
        name: aqua
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/trivy-operator/tracee
  # https://github.com/aquasecurity/tracee/blob/main/deploy/helm/tracee/values.yaml

