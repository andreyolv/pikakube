apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeclarity
  namespace: kubeclarity
spec:
  interval: 5m
  chart:
    spec:
      chart: kubeclarity
      version: 2.23.1
      sourceRef:
        kind: HelmRepository
        name: kubeclarity
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kubeclarity/kubeclarity
  # https://github.com/openclarity/kubeclarity/blob/main/charts/kubeclarity/values.yaml