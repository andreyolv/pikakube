apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ratify
  namespace: ratify
spec:
  interval: 5m
  chart:
    spec:
      chart: ratify
      version: 1.14.0
      sourceRef:
        kind: HelmRepository
        name: ratify
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/ratify/ratify
  # https://github.com/ratify-project/ratify/blob/dev/charts/ratify/values.yaml
