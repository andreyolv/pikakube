apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: presto
  namespace: presto
spec:
  interval: 5m
  chart:
    spec:
      chart: presto
      version: 0.3.0
      sourceRef:
        kind: HelmRepository
        name: presto
        namespace: flux-system
  values:
  # https://github.com/prestodb/presto-helm-charts/blob/main/charts/presto/values.yaml
  