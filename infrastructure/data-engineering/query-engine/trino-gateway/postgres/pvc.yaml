apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres
  namespace: trino-gateway
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi