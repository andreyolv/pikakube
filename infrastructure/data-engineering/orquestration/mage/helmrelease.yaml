apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mage
  namespace: mage
spec:
  interval: 5m
  chart:
    spec:
      chart: mage
      version: 0.2.6
      sourceRef:
        kind: HelmRepository
        name: mageai
        namespace: flux-system
  values:
  # https://github.com/mage-ai/helm-charts/blob/master/charts/mageai/values.yaml