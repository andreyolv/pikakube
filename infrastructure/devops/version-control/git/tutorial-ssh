TUTORIAL ADICIONANDO CHAVE SSH NO GIT
No terminal digitar os comandos um de cada vez:
ssh-keygen -t ed25519 -C "digite_seu_email@example.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

Copie o código que vai aparecer na tela acima acima

No terminal digitar os comandos um de cada vez para configurar seu git, caso seja primeira vez que esteja usando o git:
git config --global user.name "Coloque seu nome aqui"
git config --global user.email digite_seu_email@exemplo.br

Depois vá no seu github em https://github.com/settings/keys em New SSH Key pra adicionar a chave, e cole o código que copiou anteriormente.
No botão Configure SSO clique em Authorize no repositório pra autorizar.

Pronto, o git clone vai funcionar.
