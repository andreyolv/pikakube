apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitness
  namespace: gitness
spec:
  interval: 5m
  chart:
    spec:
      chart: charts/gitness
      sourceRef:
        kind: GitRepository
        name: gitness
        namespace: flux-system
  values:
  # https://github.com/harness/gitness/blob/main/charts/gitness/values.yaml