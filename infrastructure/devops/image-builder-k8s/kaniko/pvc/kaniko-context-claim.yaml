apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kaniko-context-claim
  namespace: kaniko
spec:
  storageClassName: kaniko-context
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
