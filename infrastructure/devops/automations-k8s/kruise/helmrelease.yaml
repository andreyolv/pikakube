apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kruise
  namespace: kruise
spec:
  interval: 5m
  chart:
    spec:
      chart: kruise
      version: 1.7.3
      sourceRef:
        kind: HelmRepository
        name: openkruise
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/openkruise/kruise
