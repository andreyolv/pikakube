apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mayfly
  namespace: mayfly
spec:
  interval: 5m
  chart:
    spec:
      chart: mayfly
      version: 1.1.0
      sourceRef:
        kind: HelmRepository
        name: nccloud
        namespace: flux-system
  values:
  # https://github.com/NCCloud/charts/blob/main/charts/mayfly/values.yaml