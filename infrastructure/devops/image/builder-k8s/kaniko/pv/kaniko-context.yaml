apiVersion: v1
kind: PersistentVolume
metadata:
  name: kaniko-context
  namespace: kaniko
spec:
  storageClassName: kaniko-context
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /kaniko-context
