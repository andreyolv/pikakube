apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: k8s-image-swapper
  namespace: k8s-image-swapper
spec:
  interval: 5m
  chart:
    spec:
      chart: k8s-image-swapper
      version: 1.11.0
      sourceRef:
        kind: HelmRepository
        name: estahn
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/estahn/k8s-image-swapper
  # https://github.com/estahn/charts/blob/main/charts/k8s-image-swapper/values.yaml
