apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: kraken
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: v0.1.4
  url: https://github.com/uber/kraken.git
  sparseCheckout:
  - helm