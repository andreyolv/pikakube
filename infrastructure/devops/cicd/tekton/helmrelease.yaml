apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tekton
  namespace: tekton
spec:
  interval: 5m
  chart:
    spec:
      chart: charts/tekton-operator
      sourceRef:
        kind: GitRepository
        name: tekton
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/olm/community-operators/tektoncd-operator
  # https://github.com/tektoncd/operator/blob/main/charts/tekton-operator/values.yaml
