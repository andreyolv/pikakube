apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubernetes-replicator
  namespace: resources-replicator
spec:
  interval: 5m
  chart:
    spec:
      chart: kubernetes-replicator
      version: 2.9.2
      sourceRef:
        kind: HelmRepository
        name: mittwald
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kubernetes-replicator/kubernetes-replicator
  # https://github.com/mittwald/kubernetes-replicator/blob/master/deploy/helm-chart/kubernetes-replicator/values.yaml