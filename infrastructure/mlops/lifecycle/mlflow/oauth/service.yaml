apiVersion: v1
kind: Service
metadata:
  labels:
    app: oauth-pusher
  name: oauth-pusher
  namespace: mlflow
spec:
  ports:
  - name: "4180"
    port: 4180
  selector:
    app: oauth-pusher
status:
  loadBalancer: {}