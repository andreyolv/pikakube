apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mlflow-postgres
  namespace: mlflow
spec:
  volumeMode: Filesystem
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi