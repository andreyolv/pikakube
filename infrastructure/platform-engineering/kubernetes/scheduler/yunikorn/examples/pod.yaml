apiVersion: v1
kind: Pod
metadata:
  name: ubuntu-pod
  namespace: teste
  annotations:
    yunikorn.apache.org/namespace.enableYunikorn
spec:
  containers:
  - name: ubuntu-container
    image: ubuntu:latest
    command: ["sleep", "3600"]
