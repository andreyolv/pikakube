apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: spin-operator
  namespace: spin-operator
spec:
  dependsOn:
  - name: cert-manager
    namespace: cert-manager
  - name: kwasm-operator
    namespace: kwasm
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: spin-operator
    namespace: flux-system
  values:
  # https://github.com/spinkube/spin-operator/blob/main/charts/spin-operator/values.yaml
