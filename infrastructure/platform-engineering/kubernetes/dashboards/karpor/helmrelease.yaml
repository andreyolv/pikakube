apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: karpor
  namespace: karpor
spec:
  interval: 5m
  chart:
    spec:
      chart: karpor
      version: 0.7.6
      sourceRef:
        kind: HelmRepository
        name: kusionstack
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kusionstack/karpor
  # https://github.com/KusionStack/charts/blob/master/charts/karpor/values.yaml
