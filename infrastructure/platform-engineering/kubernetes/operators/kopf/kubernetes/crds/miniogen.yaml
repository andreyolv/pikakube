apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: miniogens.kopf.dev
spec:
  scope: Namespaced
  group: kopf.dev
  names:
    kind: MinioGen
    plural: miniogens
    singular: miniogen
    shortNames:
      - mg
  versions:
    - name: v1
      served: true
      storage: true
      subresources: { status: { } }
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
      additionalPrinterColumns:
        - name: host
          type: string
          priority: 0
          jsonPath: .spec.source.host
          description: Entpoint do Minio para conexão
        - name: access_key
          type: string
          priority: 0
          jsonPath: .spec.source.access_key
          description: Access Key do Minio para conexão
        - name: secret_key
          type: string
          priority: 0
          jsonPath: .spec.source.secret_key
          description: Access Secret do Minio para conexão
        - name: bucket
          type: string
          priority: 0
          jsonPath: .spec.source.bucket
          description: Bucket no lake
        - name: path
          type: string
          priority: 0
          jsonPath: .spec.source.path
          description: Caminho no bucket

        - name: quantity
          type: integer
          priority: 0
          jsonPath: .spec.datagen.quantity
          description: Quantos registros a serem gerados
        - name: interval
          type: integer
          priority: 0
          jsonPath: .spec.datagen.interval
          description: A cada quanto tempo em segundos os registros são gerados
        - name: fields
          type: string
          priority: 0
          jsonPath: .spec.datagen.fields
          description: Quais campos a serem gerados
        