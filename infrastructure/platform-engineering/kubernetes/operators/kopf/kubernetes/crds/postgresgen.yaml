apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: postgresgen.kopf.dev
spec:
  scope: Namespaced
  group: kopf.dev
  names:
    kind: PostgresGen
    plural: postgresgens
    singular: postgresgen
    shortNames:
      - pg
  versions:
    - name: v1
      served: true
      storage: true
      subresources: { status: { } }
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
      additionalPrinterColumns:
        - name: conn-string
          type: string
          priority: 0
          jsonPath: .spec.source.conn-string
          description: Connection String para conectar na fonte de dados
        - name: table
          type: string
          priority: 0
          jsonPath: .spec.source.table
          description: Tabela no banco
    
        - name: records
          type: integer
          priority: 0
          jsonPath: .spec.datagen.records
          description: Quantos registros a serem gerados
        - name: interval
          type: integer
          priority: 0
          jsonPath: .spec.datagen.interval
          description: A cada quanto tempo os registros são gerados
        - name: field
          type: list
          priority: 0
          jsonPath: .spec.datagen.interval
          description: Quais campos a serem gerados       