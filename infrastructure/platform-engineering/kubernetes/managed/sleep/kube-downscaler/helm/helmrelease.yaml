apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-downscaler
  namespace: kube-system
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: kube-downscaler
    namespace: kube-system
  values:
  # https://artifacthub.io/packages/helm/deliveryhero/kube-downscaler
  # https://github.com/deliveryhero/helm-charts/blob/master/stable/kube-downscaler/values.yaml
