apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: koordinator
  namespace: koordinator
spec:
  interval: 5m
  chart:
    spec:
      chart: koordinator
      version: 1.5.0
      sourceRef:
        kind: HelmRepository
        name: koordinator-sh
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/koordinator/koordinator