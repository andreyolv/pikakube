apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: armada-operator
  namespace: armada
spec:
  interval: 5m
  chart:
    spec:
      chart: armada-operator
      #version: 
      sourceRef:
        kind: HelmRepository
        name: gresearch
        namespace: flux-system
  values:
  # https://github.com/armadaproject/armada-operator/blob/main/charts/armada-operator/values.yaml