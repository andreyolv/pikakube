apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubetail
  namespace: kubetail
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: kubetail
    namespace: kubetail
  values:
  # https://artifacthub.io/packages/helm/kubetail/kubetail
  # https://github.com/kubetail-org/helm-charts/blob/main/charts/kubetail/values.yaml
