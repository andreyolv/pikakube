apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: admiralty
  namespace: admiralty
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: admiralty
    namespace: flux-system
  values:
  # https://github.com/admiraltyio/admiralty/blob/master/charts/multicluster-scheduler/values.yaml
