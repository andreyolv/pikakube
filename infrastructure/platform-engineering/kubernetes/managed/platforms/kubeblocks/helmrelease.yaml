apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeblocks
  namespace: kubeblocks
spec:
  interval: 5m
  chart:
    spec:
      chart: kubeblocks 
      version: 0.9.0-alpha.1
      sourceRef:
        kind: HelmRepository
        name: kubeblocks
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kubeblocks/kubeblocks
  # https://github.com/apecloud/kubeblocks/blob/main/deploy/helm/values.yaml