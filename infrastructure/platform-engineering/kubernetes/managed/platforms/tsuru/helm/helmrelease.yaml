apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tsuru
  namespace: tsuru
spec:
  interval: 5m
  chart:
    spec:
      chart: tsuru
      version: 0.8.2
      sourceRef:
        kind: HelmRepository
        name: tsuru
        namespace: flux-system
  values:
  # https://github.com/tsuru/charts/blob/main/charts/tsuru-stack/values.yaml