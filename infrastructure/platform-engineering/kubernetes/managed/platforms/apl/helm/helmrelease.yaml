apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: apl
  namespace: apl
spec:
  interval: 5m
  chart:
    spec:
      chart: apl
      version: 
      sourceRef:
        kind: HelmRepository
        name: apl
        namespace: flux-system
  values:
  # https://github.com/linode/apl-core/blob/main/chart/apl/values.yaml