apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: devtron
  namespace: devtroncd
spec:
  interval: 5m
  chart:
    spec:
      chart: devtron-operator
      version: 0.22.68
      sourceRef:
        kind: HelmRepository
        name: devtron
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/devtron/devtron-operator
  # https://github.com/devtron-labs/devtron/blob/main/charts/devtron/values.yaml