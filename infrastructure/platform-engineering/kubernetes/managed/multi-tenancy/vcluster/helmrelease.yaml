apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vcluster
  namespace: vcluster
spec:
  interval: 5m
  chart:
    spec:
      chart: vcluster
      version: 0.24.0
      sourceRef:
        kind: HelmRepository
        name: loft-sh
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/loft/vcluster
  # https://github.com/loft-sh/vcluster/blob/main/chart/values.yaml
