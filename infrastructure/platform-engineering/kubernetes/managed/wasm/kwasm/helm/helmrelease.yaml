apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kwasm-operator
  namespace: kwasm
spec:
  interval: 5m
  chart:
    spec:
      chart: kwasm-operator
      version: 0.2.3
      sourceRef:
        kind: HelmRepository
        name: kwasm
        namespace: flux-system
  values:
  # https://github.com/KWasm/kwasm-operator/blob/main/charts/kwasm-operator/values.yaml