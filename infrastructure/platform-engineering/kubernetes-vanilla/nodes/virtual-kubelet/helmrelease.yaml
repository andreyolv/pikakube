apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: virtual-kubelet
  namespace: virtual-kubelet
spec:
  interval: 5m
  chart:
    spec:
      chart: charts/virtual-kubelet
      sourceRef:
        kind: GitRepository
        name: azure-aci
        namespace: flux-system
  values:
  # https://github.com/virtual-kubelet/azure-aci/blob/master/charts/virtual-kubelet/values.yaml