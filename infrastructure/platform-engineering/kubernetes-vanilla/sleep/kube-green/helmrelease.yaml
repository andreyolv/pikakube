apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-green
  namespace: kube-green
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-green
      version: 0.5.2
      sourceRef:
        kind: HelmRepository
        name: 
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/olm/community-operators/kube-green
  # https://github.com/kube-green/kube-green/blob/main/charts/kube-green/values.yaml