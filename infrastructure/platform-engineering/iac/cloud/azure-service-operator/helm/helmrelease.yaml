apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: azure-service-operator
  namespace: azure-service-operator
spec:
  dependsOn:
  - name: cert-manager
    namespace: cert-manager
  interval: 5m
  chart:
    spec:
      chart: azure-service-operator
      version: 1.12.0
      sourceRef:
        kind: HelmRepository
        name: aso2
        namespace: flux-system
  values:
  # https://github.com/Azure/azure-service-operator/blob/main/charts/azure-service-operator/values.yaml