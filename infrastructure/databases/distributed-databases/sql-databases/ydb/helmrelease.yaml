apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ydb-operator
  namespace: ydb
spec:
  interval: 5m
  chart:
    spec:
      chart: ydb-operator
      version: 0.5.23
      sourceRef:
        kind: HelmRepository
        name: ydb
        namespace: flux-system
  values:
  # https://github.com/ydb-platform/ydb-kubernetes-operator/blob/master/deploy/ydb-operator/values.yaml