apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: 
  namespace: 
spec:
  interval: 5m
  chart:
    spec:
      chart: 
      version:
      sourceRef:
        kind: HelmRepository
        name: 
        namespace: flux-system
  values:
  # https://github.com/schemahero/schemahero-helm/blob/main/values.yaml
