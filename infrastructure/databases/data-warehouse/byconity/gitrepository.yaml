apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: byconity
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: v0.2.0
  url: https://github.com/ByConity/byconity-deploy.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/chart/byconity