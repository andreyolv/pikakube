apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: byconity
  namespace: byconity
spec:
  interval: 5m
  chart:
    spec:
      chart: chart/byconity
      sourceRef:
        kind: GitRepository
        name: byconity
        namespace: flux-system
  values:
  # https://github.com/ByConity/byconity-deploy/blob/master/chart/byconity/values.yaml
   