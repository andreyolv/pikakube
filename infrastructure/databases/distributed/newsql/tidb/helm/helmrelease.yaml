apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tidb-operator
  namespace: tidb
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: tidb-operator
    namespace: tidb
  values:
  # https://github.com/pingcap/tidb-operator/blob/master/charts/tidb-operator/values.yaml
