apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: atlas
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  type: ClusterIP