apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: questdb
  namespace: questdb
spec:
  interval: 5m
  chart:
    spec:
      chart: questdb
      version: 0.40.4
      sourceRef:
        kind: HelmRepository
        name: questdb
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/questdb/questdb
  # https://github.com/questdb/questdb-kubernetes/blob/master/charts/questdb/values.yaml
  