apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis-cluster
  namespace: redis
spec:
  interval: 5m
  chart:
    spec:
      chart: redis-cluster
      version: 11.5.0
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/bitnami/redis-cluster
  # https://github.com/bitnami/charts/blob/main/bitnami/redis-cluster/values.yaml

    