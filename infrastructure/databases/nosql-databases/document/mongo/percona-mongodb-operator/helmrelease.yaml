apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: psmdb-operator
  namespace: percona-mongodb
spec:
  interval: 5m
  chart:
    spec:
      chart: psmdb-operator
      version: 1.19.1
      sourceRef:
        kind: HelmRepository
        name: percona
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/olm/community-operators/percona-server-mongodb-operator
  # https://github.com/percona/percona-helm-charts/blob/main/charts/psmdb-operator/values.yaml
