apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: inlong
  namespace: inlong
spec:
  interval: 5m
  chart:
    spec:
      chart: docker/kubernetes
      sourceRef:
        kind: GitRepository
        name: inlong
        namespace: flux-system
  values:
  # https://github.com/apache/inlong/blob/master/docker/kubernetes/values.yaml