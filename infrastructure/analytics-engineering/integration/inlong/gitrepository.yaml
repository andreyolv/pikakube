apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: inlong
  namespace: flux-system
spec:
  interval: 24h
  ref:
    branch: release-2.0.0
  url: https://github.com/apache/inlong.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/docker/kubernetes