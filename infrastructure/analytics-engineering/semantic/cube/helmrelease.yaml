apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cube
  namespace: cube
spec:
  interval: 5m
  chart:
    spec:
      chart: cube
      version: 2.3.1
      sourceRef:
        kind: HelmRepository
        name: gadsme
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/gadsme/cube
  # https://github.com/gadsme/charts/blob/main/charts/cube/values.yaml