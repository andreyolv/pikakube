apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mailpit
  namespace: mailpit
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: mailpit
    namespace: mailpit
  values:
  # https://artifacthub.io/packages/helm/jouve/mailpit
  # https://github.com/jouve/charts/blob/main/charts/mailpit/values.yaml
