apiVersion: v1
kind: Service
metadata:
  name: pypi-server
  namespace: pypi-server
  labels:
    app: pypi-server
spec:
  selector:
    app: pypi-server
  ports:
    - protocol: TCP
      port: 80
  type: ClusterIP