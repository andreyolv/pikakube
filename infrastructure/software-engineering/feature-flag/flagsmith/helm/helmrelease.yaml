apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flagsmith
  namespace: flagsmith
spec:
  interval: 5m
  chart:
    spec:
      chart: flagsmith
      version: 0.61.0
      sourceRef:
        kind: HelmRepository
        name: flagsmith
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/flagsmith/flagsmith
  # https://github.com/Flagsmith/flagsmith-charts/blob/main/charts/flagsmith/values.yaml