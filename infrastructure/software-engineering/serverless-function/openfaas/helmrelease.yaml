apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openfaas
  namespace: openfaas
spec:
  interval: 5m
  chart:
    spec:
      chart: openfaas
      version: 14.2.90
      sourceRef:
        kind: HelmRepository
        name: openfaas
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/openfaas/openfaas
  # https://github.com/openfaas/faas-netes/blob/master/chart/openfaas/values.yaml
    functionNamespace: openfaas
    # port-forward gateway, login in secret basic-auth
    #dashboard:
    #  enabled: true
    #  publicURL: "localhost"