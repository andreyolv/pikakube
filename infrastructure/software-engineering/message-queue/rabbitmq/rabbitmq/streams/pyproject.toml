[tool.poetry]
name = "rabbitmq-streams"
version = "0.1.0"
description = ""
authors = ["Andrey Oliveira <andreyyolv@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
rstream = "^0.20.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
