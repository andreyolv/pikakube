apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kaito
  namespace: kaito
spec:
  interval: 5m
  chart:
    spec:
      chart: charts/kaito
      sourceRef:
        kind: GitRepository
        name: kaito
        namespace: flux-system
  values:
  # https://github.com/kaito-project/kaito/blob/main/charts/kaito/workspace/values.yaml
