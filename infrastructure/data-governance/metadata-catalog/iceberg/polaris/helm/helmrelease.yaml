apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: polaris
  namespace: polaris
spec:
  interval: 5m
  chart:
    spec:
      chart: polaris
      version: 1.6.0
      sourceRef:
        kind: HelmRepository
        name: polaris
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/apache-polaris/polaris
  # https://github.com/apache/polaris/blob/main/helm/polaris/values.yaml
  