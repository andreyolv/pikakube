apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grai
  namespace: grai
spec:
  interval: 5m
  chart:
    spec:
      chart: grai
      version: 0.1.7
      sourceRef:
        kind: HelmRepository
        name: grai
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/grai/grai
  # https://github.com/grai-io/grai-helm/blob/master/charts/grai-helm/values.yaml