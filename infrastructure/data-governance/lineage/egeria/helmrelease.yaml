apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: egeria
  namespace: egeria
spec:
  interval: 5m
  chart:
    spec:
      chart: egeria-base
      version: 
      sourceRef:
        kind: HelmRepository
        name: egeria
        namespace: flux-system
  values:
  # https://github.com/odpi/egeria-charts/blob/main/charts/egeria-base/values.yaml
