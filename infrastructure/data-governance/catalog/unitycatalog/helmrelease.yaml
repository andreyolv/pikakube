apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: unitycatalog
  namespace: unitycatalog
spec:
  interval: 5m
  chart:
    spec:
      chart: helm
      sourceRef:
        kind: GitRepository
        name: unitycatalog
        namespace: flux-system
  values:
  # https://github.com/unitycatalog/unitycatalog/blob/v0.3.0/helm/values.yaml
