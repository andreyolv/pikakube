apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openmetadata
  namespace: openmetadata
spec:
  dependsOn:
  - name: openmetadata-dependencies
  interval: 5m
  chart:
    spec:
      chart: openmetadata
      version: 1.6.7
      sourceRef:
        kind: HelmRepository
        name: open-metadata
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/open-metadata/openmetadata
  # https://github.com/open-metadata/openmetadata-helm-charts/blob/main/charts/openmetadata/values.yaml
