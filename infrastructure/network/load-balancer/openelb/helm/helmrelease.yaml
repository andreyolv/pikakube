apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: openelb
  namespace: openelb
spec:
  interval: 5m
  chart:
    spec:
      chart: openelb
      version: 0.6.0
      sourceRef:
        kind: HelmRepository
        name: openelb
        namespace: flux-system
  values:
  # https://github.com/openelb/openelb/blob/master/charts/values.yaml
  