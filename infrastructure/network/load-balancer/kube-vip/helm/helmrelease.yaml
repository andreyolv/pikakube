apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-vip
  namespace: kube-vip
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-vip
      version: 0.6.4
      sourceRef:
        kind: HelmRepository
        name: kube-vip
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kube-vip/kube-vip
  # https://github.com/kube-vip/helm-charts/blob/main/charts/kube-vip/values.yaml