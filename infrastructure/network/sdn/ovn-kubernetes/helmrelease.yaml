apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ovn-kubernetes
  namespace: ovn-kubernetes
spec:
  interval: 5m
  chart:
    spec:
      chart: helm/ovn-kubernetes
      sourceRef:
        kind: GitRepository
        name: ovn-kubernetes
        namespace: flux-system
  values:
  # https://github.com/ovn-kubernetes/ovn-kubernetes/tree/master/helm/ovn-kubernetes