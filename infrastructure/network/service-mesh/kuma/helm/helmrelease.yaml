apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kuma
  namespace: kuma
spec:
  interval: 5m
  chart:
    spec:
      chart: kuma
      version: 2.9.2
      sourceRef:
        kind: HelmRepository
        name: kuma
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kuma/kuma
  # https://github.com/kumahq/kuma/blob/master/deployments/charts/kuma/values.yaml
