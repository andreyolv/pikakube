apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubeslice
  namespace: kubeslice
spec:
  interval: 5m
  chart:
    spec:
      chart: kubeslice-controller
      version: 1.3.1
      sourceRef:
        kind: HelmRepository
        name: kubeslice
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/avesha/kubeslice-controller
  # https://github.com/kubeslice/kubeslice/blob/master/charts/kubeslice-controller/values.yaml
  