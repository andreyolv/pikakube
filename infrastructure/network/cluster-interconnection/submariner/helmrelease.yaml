apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: submariner
  namespace: submariner
spec:
  interval: 5m
  chart:
    spec:
      chart: submariner-operator
      version: 0.19.1
      sourceRef:
        kind: HelmRepository
        name: submariner
        namespace: flux-system
  values:
  # https://github.com/submariner-io/submariner-charts/blob/devel/submariner-operator/values.yaml
  