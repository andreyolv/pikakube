apiVersion: v1
kind: Service
metadata:
  name: postgres-pomerium
  namespace: pomerium
spec:
  ports:
  - name: postgres-pomerium
    port: 5432
    targetPort: 5432
  selector:
    app: postgres-pomerium