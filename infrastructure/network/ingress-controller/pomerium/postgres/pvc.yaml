apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pomerium
  namespace: pomerium
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi