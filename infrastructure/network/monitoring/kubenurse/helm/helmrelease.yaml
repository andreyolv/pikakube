apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubenurse
  namespace: kubenurse
spec:
  interval: 5m
  chart:
    spec:
      chart: helm/kubenurse
      sourceRef:
        kind: GitRepository
        name: kubenurse
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kubenurse/kubenurse
  # https://github.com/postfinance/kubenurse/blob/main/helm/kubenurse/values.yaml