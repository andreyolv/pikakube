apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: easegress
  namespace: easegress
spec:
  interval: 5m
  chart:
    spec:
      chart: helm-charts/easegress
      sourceRef:
        kind: GitRepository
        name: easegress
        namespace: flux-system
  values:
  # https://github.com/easegress-io/easegress/blob/main/helm-charts/easegress/values.yaml