apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: kgateway
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: v2.0.0
  url: https://github.com/kgateway-dev/kgateway.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/install/helm