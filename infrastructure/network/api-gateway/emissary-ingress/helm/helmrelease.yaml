apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: emissary-ingress
  namespace: emissary-ingress
spec:
  interval: 5m
  chart:
    spec:
      chart: emissary-ingress
      version: 8.9.1
      sourceRef:
        kind: HelmRepository
        name: datawire
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/datawire/emissary-ingress
  # https://github.com/emissary-ingress/emissary/blob/master/charts/emissary-ingress/values.yaml.in
    service:
      type: ClusterIP
