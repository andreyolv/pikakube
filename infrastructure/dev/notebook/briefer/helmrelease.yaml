apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: briefer
  namespace: briefer
spec:
  interval: 5m
  chart:
    spec:
      chart: briefer
      version: 0.1.8
      sourceRef:
        kind: HelmRepository
        name: briefercloud
        namespace: flux-system
  values:
  # https://github.com/briefercloud/briefer/blob/main/chart/values.yaml
  #api:
  #  env:
  #    frontendUrl: https://app.briefer.yourcompany.com
  #    apiUrl: http://api.briefer.yourcompany.com