apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: keptn
  namespace: keptn
spec:
  interval: 5m
  chart:
    spec:
      chart: keptn
      version: 0.10.0
      sourceRef:
        kind: HelmRepository
        name: keptn
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/lifecycle-toolkit/keptn
  # https://github.com/keptn/lifecycle-toolkit/blob/main/chart/values.yaml
  # user: keptn
  # password: kubectl get secret -n keptn bridge-credentials -o jsonpath="{.data.BASIC_AUTH_PASSWORD}" | base64 --decodeecode