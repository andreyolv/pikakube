apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chaosblade-operator
  namespace: chaosblade
spec:
  interval: 5m
  chart:
    spec:
      chart: deploy/helm/chaosblade-operator
      # version: from gitrepository tag
      sourceRef:
        kind: GitRepository
        name: chaosblade-operator
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/chaosblade-operator/chaosblade-operator
  # https://github.com/chaosblade-io/chaosblade-operator/blob/master/deploy/helm/chaosblade-operator/values.yaml
