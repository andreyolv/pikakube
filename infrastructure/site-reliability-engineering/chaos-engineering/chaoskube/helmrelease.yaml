apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: chaoskube
  namespace: chaoskube
spec:
  interval: 5m
  chart:
    spec:
      chart: chaoskube
      version: 0.4.0
      sourceRef:
        kind: HelmRepository
        name: chaoskube
        namespace: flux-system
  values:
  # https://github.com/linki/chaoskube/blob/master/chart/chaoskube/values.yaml