apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sloth
  namespace: sloth
spec:
  interval: 5m
  chart:
    spec:
      chart: sloth
      version: 0.12.1
      sourceRef:
        kind: HelmRepository
        name: sloth
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/sloth/sloth
  # https://github.com/slok/sloth/blob/main/deploy/kubernetes/helm/sloth/values.yaml
