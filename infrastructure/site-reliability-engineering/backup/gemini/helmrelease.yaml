apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gemini 
  namespace: gemini 
spec:
  interval: 5m
  chart:
    spec:
      chart: gemini 
      version: 2.1.3
      sourceRef:
        kind: HelmRepository
        name: fairwinds-stable 
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/fairwinds-stable/gemini
  # https://github.com/FairwindsOps/charts/blob/master/stable/gemini/values.yaml