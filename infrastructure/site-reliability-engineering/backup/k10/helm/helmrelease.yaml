apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kasten 
  namespace: kasten 
spec:
  interval: 5m
  chart:
    spec:
      chart: k10
      version: 7.0.8
      sourceRef:
        kind: HelmRepository
        name: kasten 
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kasten/k10
  # https://github.com/rancher/partner-charts/blob/main-source/charts/kasten/k10/7.0.801/values.yaml