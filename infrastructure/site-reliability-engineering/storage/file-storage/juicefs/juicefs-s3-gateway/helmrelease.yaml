apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: juicefs-s3-gateway
  namespace: juicefs
spec:
  interval: 5m
  chart:
    spec:
      chart: juicefs-s3-gateway
      version: 0.11.3
      sourceRef:
        kind: HelmRepository
        name: juicefs
        namespace: flux-system
  values:
  # https://github.com/juicedata/charts/blob/main/charts/juicefs-s3-gateway/values.yaml