apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cubefs
  namespace: cubefs
spec:
  interval: 5m
  chart:
    spec:
      chart: cubefs
      sourceRef:
        kind: GitRepository
        name: cubefs
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/cubefs/cubefs
  # https://github.com/cubefs/cubefs-helm/blob/master/cubefs/values.yaml
   