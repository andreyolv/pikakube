apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: rustfs
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: 1.0.0-beta.8
  url: https://github.com/rustfs/rustfs.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/helm