apiVersion: v1
kind: PersistentVolume
metadata:
  name: hdfs-namenode
  namespace: hdfs
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /dags/spark/hdfs
