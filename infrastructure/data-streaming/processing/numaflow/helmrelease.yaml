apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: numaflow
  namespace: numaflow
spec:
  interval: 5m
  chart:
    spec:
      chart: numaflow
      version: 0.4.0
      sourceRef:
        kind: HelmRepository
        name: numaflow
        namespace: flux-system
  values:
  # https://github.com/numaproj/helm-charts/blob/main/charts/numaflow/values.yaml