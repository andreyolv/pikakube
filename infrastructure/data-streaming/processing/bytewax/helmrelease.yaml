apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bytewax
  namespace: bytewax
spec:
  interval: 5m
  chart:
    spec:
      chart: bytewax
      version: 0.7.0
      sourceRef:
        kind: HelmRepository
        name: bytewax
        namespace: flux-system
  # https://github.com/bytewax/helm-charts/blob/main/charts/bytewax/values.yaml
  values:
