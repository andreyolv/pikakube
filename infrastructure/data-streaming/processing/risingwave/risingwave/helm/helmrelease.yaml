apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: risingwave
  namespace: risingwave
spec:
  interval: 5m
  dependsOn:
  - name: cert-manager
    namespace: cert-manager
  chart:
    spec:
      chart: risingwave
      version: 0.1.28
      sourceRef:
        kind: HelmRepository
        name: risingwavelabs
        namespace: flux-system
  values:
  # https://github.com/risingwavelabs/helm-charts/blob/main/charts/risingwave-operator/values.yaml
