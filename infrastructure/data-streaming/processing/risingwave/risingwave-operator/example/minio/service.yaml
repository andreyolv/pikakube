apiVersion: v1
kind: Service
metadata:
  name: minio
  namespace: risingwave
  labels:
    app: minio
spec:
  clusterIP: None
  ports:
  - port: 9301
    name: service
  - port: 9400
    name: console
  selector:
    app: minio