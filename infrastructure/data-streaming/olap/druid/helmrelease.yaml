apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: druid-operator
  namespace: druid-operator
spec:
  interval: 5m
  chart:
    spec:
      chart: chart/
      version: 0.3.6
      sourceRef:
        kind: GitRepository
        name: druid-operator
        namespace: flux-system
  values:
  # https://github.com/datainfrahq/druid-operator/blob/master/chart/values.yaml