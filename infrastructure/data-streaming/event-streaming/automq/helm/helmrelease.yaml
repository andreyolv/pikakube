apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: automq
  namespace: automq
spec:
  interval: 5m
  chart:
    spec:
      chart: automq-for-rocketmq
      version: 0.0.6
      sourceRef:
        kind: HelmRepository
        name: automq
        namespace: flux-system
  values:
  # https://github.com/AutoMQ/charts/blob/main/charts/automq-for-rocketmq/values.yaml