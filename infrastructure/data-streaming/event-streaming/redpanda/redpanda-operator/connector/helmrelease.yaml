apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: connectors
  namespace: redpanda
spec:
  interval: 5m
  chart:
    spec:
      chart: connectors
      version: 0.1.9
      sourceRef:
        kind: HelmRepository
        name: redpanda
        namespace: redpanda
  values:
  # https://artifacthub.io/packages/helm/redpanda-data/connectors
  # https://github.com/redpanda-data/helm-charts/blob/main/charts/connectors/values.yaml

