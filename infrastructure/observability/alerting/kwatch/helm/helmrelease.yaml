apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kwatch
  namespace: kwatch
spec:
  interval: 5m
  chart:
    spec:
      chart: kwatch
      version: 0.10.0
      sourceRef:
        kind: HelmRepository
        name: kwatch
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/kwatch/kwatch
  # https://github.com/abahmed/kwatch/blob/main/deploy/chart/values.yaml