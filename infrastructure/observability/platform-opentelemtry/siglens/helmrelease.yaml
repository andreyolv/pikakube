apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: siglens
  namespace: siglens
spec:
  interval: 5m
  chart:
    spec:
      chart: siglens
      version: 0.1.3
      sourceRef:
        kind: HelmRepository
        name: siglens-repo
        namespace: flux-system
  values:
  # https://github.com/siglens/charts/blob/main/charts/siglens/values.yaml