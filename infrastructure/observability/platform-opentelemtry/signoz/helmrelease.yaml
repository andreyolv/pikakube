apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: signoz
  namespace: signoz
spec:
  interval: 5m
  chart:
    spec:
      chart: signoz
      version: 0.40.0
      sourceRef:
        kind: HelmRepository
        name: signoz
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/signoz/signoz
  # https://github.com/SigNoz/charts/blob/main/charts/signoz/values.yaml
