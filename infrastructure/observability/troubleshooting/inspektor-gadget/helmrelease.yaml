apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: inspektor-gadget
  namespace: inspektor-gadget
spec:
  interval: 5m
  chart:
    spec:
      chart: gadget 
      version: 0.35.0
      sourceRef:
        kind: HelmRepository
        name: gadget
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/gadget/gadget
  # https://github.com/inspektor-gadget/inspektor-gadget/blob/main/charts/gadget/values.yaml