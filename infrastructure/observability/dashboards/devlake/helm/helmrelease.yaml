apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: devlake
  namespace: devlake
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: devlake
    namespace: devlake
  values:
  # https://artifacthub.io/packages/helm/apache-incubator-devlake-helm-chart/devlake
  # https://github.com/apache/incubator-devlake-helm-chart/blob/main/charts/devlake/values.yaml
    lake:
      encryptionSecret:
        # to generate secret run command below
        # openssl rand -base64 2000 | tr -dc 'A-Z' | fold -w 128 | head -n 1
        secret: DSGYFHTZAYKXZMUCGQIRJJJTIYKFUDUCZIRHNCQTNWDERZBGZAWOWLBTGIFJGWROVNQZKPPPXWYOCXZOWBTLJJFVWUVRXGSKESKEBNSDNYUQTRQVTVPCYTKASISIUUWI
