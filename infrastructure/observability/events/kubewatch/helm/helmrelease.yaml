apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubewatch
  namespace: kubewatch
spec:
  interval: 5m
  chart:
    spec:
      chart: kubewatch
      version: 3.4.0
      sourceRef:
        kind: HelmRepository
        name: robusta
        namespace: flux-system
  values:
  # https://github.com/robusta-dev/kubewatch/blob/master/helm/kubewatch/values.yaml