apiVersion: v1
kind: Service
metadata:
  name: federated-prometheus
  namespace: prometheus
  labels:
    prometheus: federated-prometheus
spec:
  type: ClusterIP
  ports:
    - name: web
      port: 9090
      targetPort: 9090
  selector:
    prometheus: federated-prometheus
