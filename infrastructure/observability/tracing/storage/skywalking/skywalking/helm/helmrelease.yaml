apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: skywalking
  namespace: skywalking
spec:
  interval: 5m
  chart:
    spec:
      chart: chart/skywalking
      sourceRef:
        kind: GitRepository
        name: skywalking-helm
        namespace: flux-system
  values:
  # https://github.com/apache/skywalking-helm/blob/master/chart/skywalking/values.yaml