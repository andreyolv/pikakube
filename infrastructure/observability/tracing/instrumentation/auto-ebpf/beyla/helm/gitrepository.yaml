apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: beyla
  namespace: flux-system
spec:
  interval: 24h
  ref:
    tag: v1.9.4
  url: https://github.com/grafana/beyla.git
  ignore: |
    # exclude all
    /*
    # include deploy dir
    !/charts/beyla