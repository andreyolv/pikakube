apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: zipkin
  namespace: zipkin
spec:
  interval: 5m
  chart:
    spec:
      chart: zipkin
      version: 0.3.4
      sourceRef:
        kind: HelmRepository
        name: zipkin
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/zipkin/zipkin
  # https://github.com/openzipkin/zipkin-helm/blob/master/charts/zipkin/values.yaml
