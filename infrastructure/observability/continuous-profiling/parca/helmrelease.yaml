apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: parca
  namespace: parca
spec:
  interval: 5m
  chart:
    spec:
      chart: parca
      version: 4.19.0
      sourceRef:
        kind: HelmRepository
        name: parca
        namespace: flux-system
  values:
  # https://artifacthub.io/packages/helm/parca/parca
  # https://github.com/parca-dev/helm-charts/blob/master/charts/parca/values.yaml
