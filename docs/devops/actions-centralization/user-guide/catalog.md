# Catalog

## What exists
- Reusable workflows (CI, CD, lint, build, release).
- Composite actions for common tasks.
- Versioned tags with release notes.

## How to consume
- Reference workflows via `uses: org/repo/.github/workflows/workflow.yml@tag`.
- Adopt composite actions for repeated steps.
- Prefer pinned tags and upgrade via controlled version bumps.
